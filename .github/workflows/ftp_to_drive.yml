name: FTP to Google Drive

on:
  workflow_dispatch:
  schedule:
    - cron: "15 3 * * *"

jobs:
  run:
    runs-on: ubuntu-latest
    env:
      PYTHONUNBUFFERED: "1"   # logy hned
      MAX_DEPTH: "8"          # když chceš, lze upravit hloubku průchodu

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install pydrive2

      - name: Show script head (debug)
        run: |
          ls -la
          echo "===== fetch_and_upload.py (first 80 lines) ====="
          sed -n '1,80p' fetch_and_upload.py

      - name: Fetch from FTP and upload to Drive
        env:
          FTP_HOST: ${{ secrets.FTP_HOST }}
          FTP_USER: ${{ secrets.FTP_USER }}
          FTP_PASS: ${{ secrets.FTP_PASS }}
          GDRIVE_SA_JSON: ${{ secrets.GDRIVE_SA_JSON }}
          GDRIVE_FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}
        run: python fetch_and_upload.py
